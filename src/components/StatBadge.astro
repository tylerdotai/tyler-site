---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="flex flex-col">
  <span class="text-4xl font-serif" style="color:#00F5FF">{value}</span>
  <span class="text-sm text-[#8EA3B8] mt-1">{label}</span>
</div>
