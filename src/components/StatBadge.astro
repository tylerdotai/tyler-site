---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="flex flex-col">
  <span class="text-4xl font-serif" style="color:#FF6B00">{value}</span>
  <span class="text-sm text-[#6B6B6B] mt-1">{label}</span>
</div>
