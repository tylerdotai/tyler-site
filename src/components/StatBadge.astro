---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="flex flex-col">
  <span class="text-4xl font-serif" style="color:#C9A84C">{value}</span>
  <span class="text-sm text-[#888888] mt-1">{label}</span>
</div>
