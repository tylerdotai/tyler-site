{
  "name": "tylerdotai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "echo 'lint disabled — use astro check'",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "coverage": "vitest run --coverage",
    "ci": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.3",
    "tailwindcss": "^4.1.3",
    "@tailwindcss/vite": "^4.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.10",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.0"
  }
}
